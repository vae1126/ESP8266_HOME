config MQTT_BROKER_URL
    string "MQTT Broker URL"
    default "mqtt://47.121.26.212:1883"
    help
        MQTT broker address in mqtt://host:port format.
        Can be overridden at runtime via device command.
